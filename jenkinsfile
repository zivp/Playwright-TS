pipeline {
   agent { docker { image 'mcr.microsoft.com/playwright:focal' } }
   stages {
      stage('e2e-tests') {
         steps {
            bat 'npm install'
            bat 'npm run test'
         }
      }
   }
}